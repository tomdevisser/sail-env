server {
    listen 443 ssl;
    server_name test.sail;

    ssl_certificate     /etc/nginx/cert.pem;
    ssl_certificate_key /etc/nginx/key.pem;

    location / {
        return 200 '✅ Reverse proxy is working!';
        add_header Content-Type text/plain;
    }
}
